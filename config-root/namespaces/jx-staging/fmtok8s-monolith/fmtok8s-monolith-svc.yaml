# Source: fmtok8s-monolith/templates/service.yaml
apiVersion: v1
kind: Service
metadata:
  name: fmtok8s-monolith
  labels:
    chart: "fmtok8s-monolith-0.0.22"
    gitops.jenkins-x.io/pipeline: 'namespaces'
  annotations:
    fabric8.io/expose: "true"
    fabric8.io/ingress.annotations: 'kubernetes.io/ingress.class: nginx'
  namespace: jx-staging
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 8080
      protocol: TCP
      name: http
  selector:
    app: fmtok8s-monolith-fmtok8s-monolith
